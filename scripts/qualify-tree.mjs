import {readFile,writeFile} from 'node:fs/promises';
const raw=JSON.parse(await readFile(process.argv[2],'utf8')),initial=raw.initial_balances,failures=[];
const rows=raw.runs.map(run=>({exact:run.exact,years:run.samples.at(-1).year,maxRelativeEnergy:Math.max(...run.samples.map(s=>Math.abs((s.balances[0]-initial[0])/initial[0]))),maxAngularResidual:Math.max(...run.samples.map(s=>Math.abs(s.balances[1]-initial[1]))),maxMomentumResidual:Math.max(...run.samples.map(s=>Math.hypot(s.balances[2]-initial[2],s.balances[3]-initial[3])))}));
const trajectory=raw.runs[0].samples.map((sample,index)=>{const reference=raw.runs[1].samples[index];let p=0;for(let i=5;i<sample.state.length;i+=5)p+=(sample.state[i]-reference.state[i])**2+(sample.state[i+1]-reference.state[i+1])**2;return {year:sample.year,rmsPositionAu:Math.sqrt(p/(raw.bodies-1))};});
for(const row of rows)if(row.years!==600||row.maxRelativeEnergy>2e-5||row.maxAngularResidual>1e-9||row.maxMomentumResidual>1e-9)failures.push(`Persistent tree balance gate failed: exact=${row.exact}`);
if(trajectory.some(s=>s.rmsPositionAu>1e-3))failures.push('Tree/direct trajectory RMS exceeds 0.001 AU');
const report={schema:1,physics:raw.physics,bodies:raw.bodies,debrisEarthMasses:raw.debris_earth_masses,scope:'Constant 512-body collisionless cold disk; full 600-year tree lifetime, independently summed direct-force comparison at the same timestep. Low debris mass isolates force accumulation from hard encounters.',rows,trajectory,passed:!failures.length,failures};await writeFile('tree-qualification.json',JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(report,null,2));if(failures.length)process.exitCode=1;
